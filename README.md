# Chat API

REST API для управления чатами и сообщениями, построенное на Go с использованием GORM и PostgreSQL.

## 🚀 Быстрый запуск

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd chat-api

# Запустите приложение с помощью Docker Compose
docker-compose up --build
```

Приложение будет доступно по адресу: http://localhost:8080

## 💻 Локальный запуск (без Docker)

```bash
# 1. Установите зависимости
go mod download

# 2. Запустите PostgreSQL локально или через Docker
docker run -d \
  --name postgres-chat \
  -e POSTGRES_DB=chat_api \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=password \
  -p 5432:5432 \
  postgres:15-alpine

# 3. Соберите и запустите приложение
go build -o bin/chat-api .
./bin/chat-api

# Или запустите напрямую
go run .
```

## 📋 API Endpoints

### Чаты

#### Создать чат
```http
POST /chats
Content-Type: application/json

{
  "title": "Название чата"
}
```

**Response (201):**
```json
{
  "id": 1,
  "title": "Название чата",
  "created_at": "2026-01-16T10:00:00Z"
}
```

#### Получить чат с сообщениями
```http
GET /chats/{id}?limit=20
```

**Query Parameters:**
- `limit` (optional): Количество сообщений (по умолчанию 20, максимум 100)

**Response (200):**
```json
{
  "id": 1,
  "title": "Название чата",
  "created_at": "2026-01-16T10:00:00Z",
  "messages": [
    {
      "id": 1,
      "chat_id": 1,
      "text": "Текст сообщения",
      "created_at": "2026-01-16T10:01:00Z"
    }
  ]
}
```

#### Удалить чат
```http
DELETE /chats/{id}
```

**Response (204):** No Content

### Сообщения

#### Отправить сообщение в чат
```http
POST /chats/{id}/messages
Content-Type: application/json

{
  "text": "Текст сообщения"
}
```

**Response (201):**
```json
{
  "id": 1,
  "chat_id": 1,
  "text": "Текст сообщения",
  "created_at": "2026-01-16T10:01:00Z"
}
```

## 🔧 Требования

### Для запуска в Docker:
- Docker
- Docker Compose

### Для локальной разработки:
- Go 1.25+
- PostgreSQL (или Docker для БД)

## 🏗️ Архитектура проекта

Проект построен по принципам **чистой архитектуры** с четким разделением ответственности:

```
chat-api/
├── main.go                 # Точка входа приложения
├── handlers/               # Presentation слой - HTTP API
│   ├── handler.go          # HTTP обработчики
│   ├── router.go           # Маршрутизация запросов
│   ├── routes.go           # Определение маршрутов
│   └── middleware.go       # HTTP middleware (logging)
├── service/                # Business слой - бизнес-логика
│   └── service.go          # Сервисы приложения
├── repository/             # Data слой - работа с БД
│   └── repository.go       # Репозитории данных
├── logger/                 # Логирование
│   ├── base.go             # Базовый логгер
│   ├── database.go         # Логгер БД операций
│   └── request.go          # Логгер HTTP запросов
├── models/                 # Domain модели и DTO
│   ├── chat.go             # Модель чата
│   ├── message.go          # Модель сообщения
│   └── dto.go              # Data Transfer Objects
├── database/               # Конфигурация базы данных
│   └── database.go         # Подключение к PostgreSQL
├── utils/                  # Утилиты
│   └── env.go              # Работа с переменными окружения
├── migrations/             # Миграции базы данных
│   └── 001_create_chats_and_messages.sql
├── tests/                  # Интеграционные тесты
│   └── integration_test.go # Полноценные E2E тесты
├── Dockerfile              # Docker образ приложения
├── docker-compose.yml      # Production среда
├── docker-compose.test.yml # Тестовая среда
├── .gitignore              # Исключаемые файлы
├── go.mod                  # Go модули
└── README.md               # Документация
```

## 🗄️ База данных

Проект использует PostgreSQL с автоматической миграцией при запуске.

### Схема базы данных

#### Таблица `chats`
- `id` (SERIAL PRIMARY KEY)
- `title` (VARCHAR(200) NOT NULL)
- `created_at` (TIMESTAMP WITH TIME ZONE)
- `updated_at` (TIMESTAMP WITH TIME ZONE)

#### Таблица `messages`
- `id` (SERIAL PRIMARY KEY)
- `chat_id` (INTEGER NOT NULL, FOREIGN KEY)
- `text` (TEXT NOT NULL)
- `created_at` (TIMESTAMP WITH TIME ZONE)
- `updated_at` (TIMESTAMP WITH TIME ZONE)

## ✅ Валидация

### Чаты
- `title`: обязательное поле, 1-200 символов, пробелы по краям триммятся

### Сообщения
- `text`: обязательное поле, 1-5000 символов, пробелы по краям триммятся

## 🧪 Тестирование

Проект включает комплексные интеграционные тесты, которые проверяют полный цикл работы API от HTTP запросов до базы данных.

### Запуск тестов

```bash
# Запуск всех тестов
go test -v ./...

# Запуск только интеграционных тестов
go test -v ./tests/...

# Запуск с покрытием кода
go test -v -coverprofile=coverage.out ./...
go tool cover -html=coverage.out -o coverage.html

# Запуск с гонками (race detector)
go test -v -race ./...
```

### Что тестируется

- ✅ **Создание чата** - POST /chats/
- ✅ **Получение чата с сообщениями** - GET /chats/{id}
- ✅ **Отправка сообщений** - POST /chats/{id}/messages/
- ✅ **Удаление чата с каскадом** - DELETE /chats/{id} (проверка удаления сообщений)
- ✅ **Каскадное удаление** сообщений при удалении чата

### Тестовая инфраструктура

- **База данных**: PostgreSQL для реалистичного тестирования
- **HTTP клиент**: Использует httptest для симуляции HTTP запросов
- **Фреймворк**: testify/suite для организации тестов
- **Миграции**: SQL миграции встроены в код тестов для автономности

### Запуск тестов

#### Вариант 1: Docker Compose (рекомендуется)
```bash
# Запуск тестовой базы данных
docker-compose -f docker-compose.test.yml up -d

# Ожидание готовности БД (healthcheck)
sleep 10

# Запуск тестов (порт 5433 настроен по умолчанию в тестах)
go test -v ./tests/...

# Остановка и очистка
docker-compose -f docker-compose.test.yml down -v
```

#### Вариант 2: Прямые Docker команды
```bash
# Запуск PostgreSQL для тестов
docker run -d \
  --name postgres-test \
  -e POSTGRES_DB=chat_api_test \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=password \
  -p 5432:5432 \
  postgres:15-alpine

# Ожидание готовности БД
sleep 5

# Запуск тестов
go test -v ./tests/...

# Очистка
docker stop postgres-test && docker rm postgres-test
```

## 🛠️ Разработка

### Сборка и запуск

```bash
# Сборка приложения
go build -o bin/chat-api .

# Запуск приложения
./bin/chat-api

# Или запуск напрямую
go run .
```

### Форматирование и линтинг

```bash
# Форматирование кода
go fmt ./...

# Установка линтера
go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

# Запуск линтера
golangci-lint run
```

### Работа с зависимостями

```bash
# Загрузка зависимостей
go mod download

# Очистка модульного кеша
go clean -modcache
```

## 🔧 Переменные окружения

| Переменная | Значение по умолчанию | Описание |
|------------|----------------------|----------|
| `DB_HOST` | `localhost` | Хост базы данных PostgreSQL |
| `DB_PORT` | `5432` | Порт базы данных |
| `DB_USER` | `postgres` | Пользователь базы данных |
| `DB_PASSWORD` | `password` | Пароль базы данных |
| `DB_NAME` | `chat_api` | Имя базы данных |
| `DB_SSLMODE` | `disable` | Режим SSL для PostgreSQL |
| `PORT` | `8080` | Порт HTTP сервера |
| `LOG_TO_FILE` | `on` | Включить логирование в файлы |

## 🔒 Ограничения и бизнес-логика

### Валидация данных:
- **Название чата**: 1-200 символов, обязательное поле
- **Текст сообщения**: 1-5000 символов, обязательное поле

### API ограничения:
- Нельзя отправить сообщение в несуществующий чат (404)
- При удалении чата полностью удаляются все связанные сообщения (CASCADE)
- Максимальное количество сообщений в ответе - 100
- Сообщения возвращаются в порядке убывания даты создания

## 📊 Логирование

### HTTP запросы:
- Метод, URI, IP адрес клиента
- Код ответа, время выполнения
- Логируются в `logs/request.log`

### Операции БД:
- Тип операции, таблица, время выполнения
- Успешные и ошибочные операции
- Логируются в `logs/database.log`

### Уровни логирования:
- **INFO**: Успешные операции
- **WARN**: Предупреждения
- **ERROR**: Ошибки выполнения

## 🏥 Health Check

```http
GET /health
```

Возвращает `200 OK` если приложение работает корректно.

## 🐳 Docker

### Сборка образа
```bash
docker build -t chat-api .
```

### Запуск с Docker Compose
```bash
docker-compose up -d
```

### Просмотр логов
```bash
docker-compose logs -f app
```